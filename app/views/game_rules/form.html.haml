= render 'shared/errors', model: @game_rule
%h2
  Rule of
  = link_to @game.name, edit_game_path(@game)
= form_for [@game, @game_rule] do |f|
  .form-group
    = f.label :description
    = f.text_area :description, class: 'form-control'
  .form-group
    = f.submit class: 'btn btn-primary'
